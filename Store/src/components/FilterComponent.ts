import { IComponent } from '../types/types';

class FilterComponent implements IComponent {
    render(): void {
        //TODO  draw filter
    }
    click(): void {
        //e.tagret = callback();
    }
}
